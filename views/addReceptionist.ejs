<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Reception</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #e9f3fb;
    }
    .form-container {
      background-color: white;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
      padding: 30px;
      margin-top: 50px;
    }
    .form-header {
      background-color: #0056b3;
      color: white;
      padding: 15px;
      border-radius: 8px 8px 0 0;
      font-size: 1.5rem;
      font-weight: bold;
      text-align: center;
    }
  </style>
</head>
<body>

  <div class="container">
    <div class="col-md-8 offset-md-2 form-container">
      <div class="form-header">Add Reception</div>
      <form>
        <div class="mb-3 mt-4">
          <label for="receptionName" class="form-label">Reception Name</label>
          <input type="text" class="form-control" id="receptionName" placeholder="Enter reception name">
        </div>

        <div class="mb-3">
          <label for="receptionContact" class="form-label">Contact</label>
          <input type="text" class="form-control" id="receptionContact" placeholder="Enter contact number">
        </div>

        <div class="mb-3">
          <label for="status" class="form-label">Status</label>
          <select class="form-select" id="status">
            <option selected disabled>Choose status</option>
            <option value="Active">Active</option>
            <option value="Inactive">Inactive</option>
            <option value="Inactive">On leave</option>
          </select>
        </div>

        <button type="submit" class="btn btn-success">Submit</button>
      </form>
    </div>
  </div>

</body>
</html>
