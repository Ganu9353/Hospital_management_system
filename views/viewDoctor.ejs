<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>View Doctors</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #e0f7fa, #ffffff);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.25);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.2);
      padding: 40px;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .card-header {
      background: rgba(13, 110, 253, 0.9);
      color: white;
      border-radius: 12px 12px 0 0;
      font-weight: 600;
      text-align: center;
      padding: 15px;
      font-size: 1.5rem;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th, td {
      text-align: left;
      padding: 12px;
      vertical-align: middle;
    }

    th {
      background-color: rgba(242, 242, 242, 0.8);
      font-weight: 600;
    }

    tr:nth-child(even) {
      background-color: rgba(255, 255, 255, 0.3);
    }

    .btn {
      border-radius: 10px;
      font-weight: 500;
    }

    .content {
      margin-left: 250px;
      padding: 20px;
      margin-top: 100px;
    }
  </style>
</head>
<body>
  <%- include('header1') %>
  <%- include('Adminsidebar') %>

  <div class="content">
    <div class="glass-card">
      <div class="card-header" style="background: rgba(13, 110, 253, 0.9);">View Doctors</div>
      <div class="table-responsive mt-4">
        <table class="table table-bordered align-middle">
          <thead>
            <tr>
              <th>Doctor Name</th>
              <th>Specialization</th>
              <th>Phone</th>
              <th>Experience (Years)</th>
              <th>Status</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% doctors.forEach(doctor => { %>
            <tr>
              <td><%= doctor.doctor_name %></td>
<td><%= doctor.doctor_specialization %></td>
<td><%= doctor.doctor_contact %></td>
<td><%= doctor.doctor_experience %></td>
<td><%= doctor.status %></td>
<td>
  <a href="/editDoctor?id=<%= doctor.doctor_id %>" class="btn btn-warning btn-sm">Edit</a>
  <a href="/deleteDoctor?id=<%= doctor.doctor_id %>" class="btn btn-danger btn-sm">Delete</a>
</td>

            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
