<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>View Patients</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #e3f2fd, #f1fdf4);
      font-family: Arial, sans-serif;
      padding: 30px;
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.25);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.2);
      padding: 40px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      margin-top: 60px;
    }

    .card-header {
      background-color: #0d6efd;
      color: white;
      padding: 15px;
      border-radius: 15px 15px 0 0;
      font-size: 1.8rem;
      font-weight: bold;
      text-align: center;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      background-color: rgba(255, 255, 255, 0.7);
      border-radius: 12px;
      overflow: hidden;
    }

    th, td {
      padding: 14px 16px;
      text-align: left;
      border-bottom: 1px solid rgba(0, 0, 0, 0.1);
    }

    th {
      background-color: #d6e0ff;
      font-weight: bold;
    }

    tr:hover {
      background-color: rgba(0, 0, 0, 0.04);
    }

    .btn-group .btn {
      margin-right: 6px;
      border-radius: 8px;
    }

    .btn-sm {
      padding: 6px 12px;
      font-size: 0.85rem;
    }
  </style>
</head>
<body>

<div class="container">
  <div class="glass-card">
    <div class="card-header">
      View Patients
    </div>
    <div class="card-body">
      <table class="table">
        <thead>
          <tr>
            <th>Patient Name</th>
            <th>Age</th>
            <th>Gender</th>
            <th>Contact</th>
            <th>Admitted Date</th>
            <th>Discharged Date</th>
            <th>Room Number</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% patients.forEach(patient => { %>
          <tr>
            <td><%= patient.name %></td>
            <td><%= patient.age %></td>
            <td><%= patient.gender %></td>
            <td><%= patient.contact %></td>
            <td><%= patient.issue %></td>
            <td><%= patient.admitDate %></td>
            <td><%= patient.dischargeDate ? patient.dischargeDate : 'N/A' %></td>
            <td><%= patient.roomNumber %></td>
            <td class="btn-group" role="group" aria-label="Actions">
              <a href="/editPatient?id=<%= patient.id %>" class="btn btn-warning btn-sm">Edit</a>
              <a href="/deletePatient?id=<%= patient.id %>" class="btn btn-danger btn-sm">Delete</a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
